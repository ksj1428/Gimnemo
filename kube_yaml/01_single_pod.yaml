apiVersion: v1
kind: Pod
metadata:
  name: web-pod-nginx
  labels:
    app : web
    app-type : nginx
spec:
  containers:
  - name: container-name
    image: nginx:latest
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 80
  - name: alpine
    image: alpine:latest
    resources:
      limits:
        memory: "256Mi"
        cpu : "500m"
  imagePullSecrets:
  - name: docker-pull-secret
